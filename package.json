{
  "name": "moving-day-root",
  "version": "1.0.0",
  "description": "Moving Day - Relocation Concierge Assistant",
  "private": true,
  "scripts": {
    "setup": "npm install && npm run install:all && npm run playwright:install",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "playwright:install": "npx --prefix backend playwright install chromium",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:demo": "concurrently \"npm run start:demo --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
